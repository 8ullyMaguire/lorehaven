# Session summary — 2026-09-10

## Where the project stands

**Lorehaven** (`/home/alvaro/code/rust/lorehaven`) — a self-hosted fanfiction
archive and community platform, built fresh from the 18-milestone plan and
deliberately sharing no code with the older FicNexus repository, so the two
approaches can be compared.

| Milestone | State |
|---|---|
| 0 — Repository, tooling, running application | Complete, tag `v0.01-running-app` |
| 1 — Design system and navigation | Complete (caveats in the open risks below) |
| 2 — Accounts, pseuds, privacy, age policy | Complete: API **and** pages, tag `v0.03-identity` |
| 3–18 | Not implemented |

`docs/requirements.csv` records every requirement with a status;
`docs/verification.md` records the evidence behind each claim.

Commits made in this session:

```
e302719  feat(frontend): the Milestone 2 pages, and the record of what they
         were checked against            → tag v0.03-identity
c7c40de  feat: Lorehaven Milestone 2 — accounts, pseuds, privacy and age policy
```

---

## Where this session started

The Milestone 2 backend was **written but uncommitted and unfinished as a
commit**: formatting had never been applied to any of the new files, and
`cargo clippy --all-targets` reported six warnings. There was no frontend for it
at all — registration, sign in, sessions, pseuds and settings were reachable only
by API.

So the session was: make the backend committable, commit it, then build the pages
it was waiting for and check them against a real browser rather than against the
tests alone.

---

## Earlier the same day

Recorded here so the summary covers the day rather than only its second half.

**Milestone 0 — the running application.** Three-crate workspace (`domain` /
`db` / `app`), dual-dialect migrations embedded in the binary, layered
configuration, structured logging with request IDs, health endpoints that
distinguish liveness from readiness, an idempotent development seeder, `doctor`,
and a Svelte frontend embedded in the executable. Tag `v0.01-running-app`.

**Milestone 1 — design system and navigation.** "The Reading Room" tokens (warm
paper, evergreen, copper) with three presets plus a system preference; the
accessible component primitives; the navigation shell with desktop and mobile
layouts. Fifteen primitives at the time; eighteen now, since Milestone 2 added
`Textarea`, `PrivacySettings` and `ContentPreferences`.

---

## What was built in this session

### Milestone 2 — the backend, made committable

`cargo fmt --all`, then the six clippy findings, each of which was a real
simplification rather than a lint silencer: a redundant `#[must_use]` on an
iterator return, a `Default::default()` followed by one field assignment, an
`or_else(|| None)` that was a no-op where a comment had been left behind, a
`for (key, _) in map` that only wanted the keys, and a single-element `for` loop
in a test. `crates/app/tests/milestone_2.rs` was then 27 tests over the real
router, a real SQLite file and a cookie jar that mirrors what a browser does.

### Milestone 2 — the pages

* **A session store** (`frontend/src/lib/session.svelte.ts`) that distinguishes
  the server saying "you have no session" from "we could not ask". The first is
  anonymous; the second leaves the status unknown and records the failure,
  because a network blip is not evidence that somebody was signed out.
* **Registration, sign in, password reset** — with the server's `field_errors`
  landing on the fields that caused them, and the request id shown so it can be
  quoted.
* **The account page** — live sessions with a revoke control per device and a
  sign-out-everywhere zone, reading preferences, and account-level privacy.
* **The pseud page** — create, edit (with the version sent back), switch which
  face acts, per-pseud privacy, and a link to each public profile.
* **A public profile page** — a hidden pseud and a pseud that never existed
  produce the same page.
* **Three components** — `Textarea`, `PrivacySettings`, `ContentPreferences` —
  and a router that resolves the new paths rather than sending them to the
  placeholder.

Two decisions worth remembering:

* The privacy forms render the key schema the server sends rather than keeping
  their own list of keys and values. A key added on the server appears in the
  interface without a frontend change; a key removed stops being offered. The
  accepted values come from the same source.
* The reading-preferences form reports the policy ceiling and the *effective*
  rating next to the choice. A preference above the ceiling is remembered and
  says plainly that it will not change what is shown, instead of appearing to
  have worked.

---

## Bugs found by testing, not by reading

Items 1–6 were found in the earlier part of the day, before this session; 7–8
came out of driving the Milestone 2 journey in a real browser, against the
compiled binary serving its embedded bundle.

1. **The rate limiter was inert in production.** `axum::serve` was given the
   router directly, so no peer address was ever available; the limiter had no
   key and skipped enforcement. Fixed by serving
   `into_make_service_with_connect_info`, *and* by making the limiter fall back
   to a shared bucket when no address can be determined — so the failure mode is
   "everyone shares one allowance" rather than "nobody is limited".
2. **`AgeState::Restricted` was not a minor state.** A restricted account is by
   construction below the age threshold, so it was silently receiving the adult
   defaults for messaging and taste learning.
3. **The class marker sat inside the limiter.** The fail-closed guard rejected
   every request on every route, because middleware runs outermost-first and the
   class was attached after the limiter rather than before it.
4. **A zero refill rate panicked.** `Duration::from_secs_f64` was handed
   infinity on the way to a retry hint.
5. **The appearance selector applied but never persisted.** `bind:value`
   combined with `onchange` left persistence dependent on listener ordering. A
   unit test on the storage module could not see it; a shell-level test can.
6. **The build identity was frozen.** `build.rs` cached the git revision, so a
   deployed binary would keep reporting the commit it was first built from.
7. **The shell kept claiming a session after a password reset.** Redeeming a
   reset token revokes every session on the account — but the header still said
   "Writing as @quill", because nothing told the session store. Fixed on the
   reset page and pinned by `frontend/src/routes/PasswordReset.test.ts`.
8. **A failed sign-out threw.** The store cleared local state and then re-threw
   the network error, which its caller could do nothing with. It now records the
   failure, so a page can say the session may still be live on the server.
   Caught by a unit test written to assert the opposite of what the code did.

---

## Verification actually performed

```text
cargo test --workspace               163 passed, 0 failed (9 binaries)
cargo clippy --all-targets
  --all-features -- -D warnings      clean
cargo fmt --all -- --check           clean
vitest (frontend)                    57 passed (10 files)
vite build                           160 modules; 103.6 kB JS (36.8 kB gzip),
                                     28.5 kB CSS
lorehaven migrate                    applied 0001_identity, 0002_sessions_and_settings
lorehaven doctor                     17 checks: 0 failing, 1 warning
lorehaven serve --port 8123          driven through the whole Milestone 2 journey
                                     in a real browser; two defects came out of it
```

The browser journey is written out step by step in `docs/verification.md`,
including the endpoints' own behaviour observed from outside the interface: a
state-changing request without a CSRF header returns `403`, and a session revoked
from the account page then gets `401` on `curl`.

Two housekeeping notes for whoever picks this up:

* The test instance used a scratch database at `/tmp/lh-e2e` with the project's
  own `data/lorehaven.sqlite` left alone.
* Port **8099** is held by a `lorehaven` process that predates this session and
  was not started here; that is why the browser work moved to 8123.

A clean checkout builds without the frontend bundle (a placeholder is
generated), and the real bundle is embedded once built.

---

## Open risks and known gaps

1. **PostgreSQL has never been executed.** Every PostgreSQL statement is
   written but unverified; there is no server on this machine. A CI job exists
   and fails loudly rather than skipping, but it has never run. This is the
   largest open risk in the project. See ADR 0004.
2. **Template type-checking is not part of the gate.** `svelte-check` cannot
   start in this checkout — it reports "No Svelte configuration found in vite
   config" for every component, including ones that predate this session. The
   Vite build (which compiles every template) and Vitest are what actually
   protect the frontend, so a mistake only a type-checker would catch, such as a
   prop of the wrong shape, would not be caught.
3. **The new pages have no automated browser coverage.** They were driven by
   hand through the whole journey; Vitest covers the store, the router, the API
   client and two pages. A Playwright suite is still missing (spec §23).
4. **No email transport.** Password reset returns the token in the response
   outside production and logs only the request inside it — honest, but not a
   shipped flow. SMTP is spec §2.2 optional infrastructure, not yet chosen.
5. **No common-password list.** Password strength is length plus the rate
   limiter, and this is stated rather than dressed up.
6. **"Every route declares a class" is a convention, not a boot-time check.**
   axum does not expose the route table, so the enforcer's fail-closed behaviour
   is unit-tested directly and the end-to-end property is tested for classified
   subtrees. A boot-time audit would need a declared route inventory — which
   spec §24 asks for anyway, and which is the natural way to close this.
7. **Blocks and mutes are still tables with no behaviour** (M2-06). The
   requirement is recorded as `unsupported` rather than implied by the schema.
8. **Two tabs can collide on the settings forms.** The server refuses a stale
   version and the interface shows the conflict with a reload, but whoever loses
   that race has to re-apply their change by hand.
9. **Milestone 1 is incomplete against its own list**: no combobox, and the
   richer work-card variants are missing. Layout at 320 px and at 200 % zoom has
   never been measured.
10. **Fonts are not self-hosted.** The intended typography appears only where
    the named families happen to be installed.
11. **`lorehaven migrate | head` panics on a broken pipe.** Cosmetic, but a
    wart; fixing it needs `unsafe` for `SIGPIPE` and the workspace forbids that.

---

## Next concrete step

Milestone 3: drafts, chapters, publishing and revision history — the first
vertical slice the plan calls for. It is the first milestone that has to use the
content-eligibility service the age policy already exposes
(`crates/domain/src/policy.rs`), which has had no caller until now, and the
optimistic-concurrency contract that `pseuds` currently exercises on its own.
