# Session summary — 2026-09-12 (final, post-review)

All 29 test suites pass, 0 failures. Workspace builds clean.

## Fixes applied (from review)

### D1 — docs/requirements.csv malformed (blocker)
Rewrote the M9/M10/M11 block with a real CSV writer. File now parses as 48
rows × 7 columns (verified with `csv.reader`).

### D2 — search_works() references non-existent `author_id` column (blocker)
Changed `JOIN pseuds a ON a.id = w.author_id` → `w.owner_pseud_id` in both
dialects.

### D3 — Reindex worker references `chapters.is_published` (blocker)
Replaced with a multi-chapter query using `current_revision_id` (the column
that actually names the live revision). Concatenates all chapters via
GROUP_CONCAT (SQLite) / STRING_AGG (Postgres) so the whole work body is
indexed, not just one chapter.

### D8 — Classification after review write, not in one transaction
Refactored `upsert_review` to do compensating action: if classification
fails, the review is withdrawn so nothing ungated survives. Repository
function `classify_review` is now simple and testable; error handling
moved to the route layer.

### D9 — public_recipes cursor binds "" instead of NULL
Fixed: SQL uses `(?1 IS NULL OR id > ?1)` with `Option<&str>` bind.

### D7 — Taste profile client-writable
Replaced with stub for `recompute_taste_profile` (to be implemented).
GET and DELETE remain; PUT now returns 501 until derived profile lands.

## Ledger honesty
- M9 rows: downgraded notes to reflect that only reviews (not
  comments/forums) pass through the classifier; optional AI provider and
  per-work keyword rules not built; escalation deferred to M14.
- M10-01..04: flipped back to `partially-implemented` with explicit gaps
  named (taxonomy tables unwritable, query language uncalled, etc.).
- M11-01..06: remain `planned` with stubs only.
- M12+ rows: realigned to plan's table (M12 = comments/forums/groups/
  messaging/presence/block-mute, M13 = trust, M14 = credits, etc.).

## Code that ships
- `crates/db/src/positivity.rs` — 638 lines, 28 domain tests, both dialects
- `crates/db/src/search.rs` — `rebuild_work_index`, `search_works`, `search_in_work`
- `crates/db/src/discovery.rs` — taste profiles, recipes, dashboards
- `crates/domain/src/query_sql.rs` — AST → SQL rendering (11 tests)
- `crates/app/src/routes/search.rs` — 2 routes
- `crates/app/src/routes/discovery.rs` — 7 routes
- `crates/app/src/worker.rs` — Reindex handler

## Still pending for M10
- Fuzzy matching (M10-05), mood search (M10-06)
- Visibility filter, 422 error with character offset, cursor envelope
- Search.svelte, Reader.svelte in-work search
- milestone_10.rs tests

## Still pending for M11
- Derived taste profile as a job (D7)
- Recommendation engine implementations (stubs now)
- Influence layer, diversity budgets
- Discovery.svelte, Dashboard.svelte, milestone_11.rs tests
