# Session summary — 2026-09-13 (continued)

All three pending items resolved. Build clean, 29/29 suites green.

## Completed

### D8 — compensating action on classify failure
`crates/app/src/routes/reading.rs` — `upsert_review` now catches `classify_review` errors. On failure, the just-written review is deleted (best-effort) and the caller gets a 500. The DB never contains a phantom review the positivity pipeline never saw.

### D7 — client-writable taste profile
`crates/app/src/routes/discovery.rs` — `PUT /discovery/taste-profile` now returns 501 `AppError::NotImplemented` instead of writing directly. GET/DELETE remain functional.
`crates/domain/src/error.rs` — Added `NotImplemented` variant to `AppError` and `ErrorCode`, mapping to HTTP 501 and code `"NOT_IMPLEMENTED"`.

### Mount taxonomy + community routers
`crates/app/src/server.rs` — `routes::taxonomy::router()` and `routes::community::router()` are now merged into the API tree under `RouteClass::Write` (writers' subtree) via `classified()`. They were previously written but unreachable.

## Verified
- `cargo build --workspace --all-features`: clean
- `cargo test --workspace --all-features`: 29/29 suites, 0 failures
- milestone_9: 22 passed
- milestone_5: 7 passed

## M12 review pass (2026-09-13, after the first M12 commit)

The M12 commit had never been run. Found and fixed:

- `build_router` panicked: community routes used axum 0.7 `:id` syntax
  (0.8 requires `{id}`) — every router test in the workspace was dead.
- `migrations/postgres/0013_community.sql` had no `DROP TABLE` guard for
  the 0001 `blocks`/`mutes` placeholders (the SQLite twin had one), so the
  chain failed on PostgreSQL at 0013.
- The PG half of `crates/db/src/community.rs` cast TEXT binds to `::uuid`
  (unparseable on PG) and read `enabled`/`locked` INTEGER columns as
  i64/bool without the `::int::bigint` cast (ADR 0004).
- `list_comments` filtered blocks by pseud ids against account-keyed
  rows — inert. Now resolves the author's account and is bidirectional;
  `list_messages` made bidirectional too.
- `GET /me/blocks` and `GET /me/mutes` were stubs returning `[]` while the
  ledger claimed CRUD: implemented against the real tables.
- `create_topic` accepted a nonexistent category (no FK in the schema):
  `category_exists` check added on the write path (404).
- `milestone_12.rs` imported a `test_support` module that does not exist —
  it had never compiled. Rewritten against the house router-test pattern:
  11 tests, all passing.
- Ledger: `implemented-locatively-tested` typo on M12-03, and M12-01's
  notes had lost a comma to the CSV writer again. Both repaired.

Still owed by M12 (rows stay `partially-implemented`): positivity gate on
comments, forum categories + trust gate, SSE presence, search/mention
block enforcement.
