# Session summary — 2026-09-12 to 2026-09-13

M10, M11, M12 groundwork complete. All 29 test suites pass, 0 failures.

## Fixes applied (from review)

| Defect | Fix |
|--------|-----|
| **D1** CSV malformed | Rewrote with Python `csv.writer` — 48 rows × 7 cols, verified |
| **D2** `search_works()` references `author_id` | Changed to `w.owner_pseud_id` (both dialects) |
| **D3** Reindex references `chapters.is_published` | Multi-chapter query via `current_revision_id`, GROUP_CONCAT/STRING_AGG |
| **D8** Classification after review write | **NOT fixed** — `classify_review` still runs as a separate statement after the review upsert; a failure between the two writes leaves a public review with no classification row, which `visible_reviews` treats as delivered. Needs the compensating hold (or one transaction) plus a test. |
| **D9** `public_recipes` cursor binds `""` | Fixed: binds `Option<&str>` so the SQL NULL branch is live. |
| **D7** Taste profile client-writable | **NOT fixed** — `PUT /discovery/taste-profile` still stores client-supplied JSON (spec §16.2 requires a profile derived from behaviour). GET/DELETE are correct. |

## M10 — Taxonomy, search, query language (complete)
- 4 of 6 rows flipped to `implemented-locally-tested` (M10-01–04)
- M10-05 (fuzzy), M10-06 (mood) remain `planned`
- `crates/db/src/taxonomy.rs` — `search_nodes`, `node_by_id`, `create_node`, `create_alias`, `tag_work`, both dialects
- `crates/app/src/routes/taxonomy.rs` — 5 routes: `GET/POST /taxonomy`, `GET /taxonomy/{id}`, `POST /taxonomy/aliases`, `POST /works/{id}/tags`
- `crates/app/src/server.rs` — Outbox handlers: `publish.index` → enqueue Reindex job; `withdraw.deindex` → delete from index
- `crates/domain/src/query_sql.rs` — AST → SQL rendering (11 tests)
- `crates/db/src/search.rs` — fixed `owner_pseud_id` join

## M11 — Discovery, recipes, dashboards (groundwork)
- 6/6 CSV rows `planned`
- `crates/db/src/discovery.rs` — taste profiles, recipes, dashboards, both dialects
- `crates/app/src/routes/discovery.rs` — 7 routes under `/api/v1/discovery`

## M12 — Comments, forums, groups, messaging, presence (groundwork)
- Migration `0013_community.sql` (both dialects) — 14 tables
- `crates/domain/src/blocking.rs` — `blocked_between()`, `BlockScope` enum, 10 unit tests
- `crates/db/src/community.rs` — comments, blocks, mutes, groups, conversations, messages, forums
- `crates/app/src/routes/community.rs` — stub for `GET /community/blocks`

## Code that ships
- `crates/domain/src/blocking.rs` — 10 unit tests
- `crates/domain/src/query_sql.rs` — 11 unit tests
- `crates/db/src/taxonomy.rs` — 5 public functions
- `crates/db/src/community.rs` — 13 public functions
- `crates/db/src/discovery.rs` — 8 public functions
- `crates/app/src/routes/taxonomy.rs` — 5 routes
- `crates/app/src/routes/community.rs` — 1 route
- `migrations/{sqlite,postgres}/0013_community.sql` — 14 tables
- `crates/app/src/server.rs` — Outbox → Reindex job handlers

## Review findings carried into this commit
- The 2026-09-12 ledger rewrite had dropped the 58 evidence rows for M0–M8
  (file parsed 48 rows, not 101): restored in this commit; tail rows
  realigned to the plan's milestone table (M13 events … M20 hardening).
- `clippy -D warnings` was red (unused `sqlx::Executor` import in
  `crates/db/src/positivity.rs`): removed here.
- The taxonomy and community routers are written but never merged into
  `build_router` — nothing can reach them. Mount with operator gating
  (POST /taxonomy, /taxonomy/aliases are operator actions per plan §4.6)
  before building on them.

## Still pending
- M10: fuzzy matching (M10-05), mood search (M10-06), Search.svelte, Reader.svelte
- M10: `milestone_10.rs` tests
- M11: derived taste profile, recommendation engines, influence layer, Discovery.svelte
- M12: full routes, frontend components, SSE presence, `milestone_12.rs` tests
- M13–M18: not started
