# 2026-09-19

## The route-audience direction test was not running (`2cec0a1`)

Reviewing the Phase 1c §2.3c work turned up that `127f72e`'s
`registered_routes_are_tabled` never compared anything:

- it panicked at HEAD — `attempt to subtract with overflow` at
  `route_inventory.rs:2485`, because the function header was skipped and a body's
  opening brace therefore never counted — so the workspace suite was **red** at
  that commit (gate log: `test_exit=101`, `registered_routes_are_tabled FAILED`,
  1227 passed);
- with the panic fixed, an instrumented run printed `COLLECT <module> 0` for all
  33 modules: the header parse read `router()` as the function's name
  (`trim_end_matches('(')` cannot strip a trailing `)`);
- with that fixed, `extract_handler` still returned `None` per route, breaking on
  the leading comma of `, get(list_media))`.

Repaired by walking backwards from each `.route(` to its enclosing declaration and
keeping the ones that return `Router<…>` — which also covers `governance.rs`'s
`fn router() { routes() }`, a shape the name list "router"/"*_routes" skipped —
plus an assertion that a module building a router yields at least one route.
Deleting `discovery.rs:get_dashboard` (a route inside `.nest("/dashboard", …)`)
now fails it by name; restoring returns 2 passed. Its first real run found three
untabled doors under `/media-collections/…`, now tabled: 303 → 306 rows.

## M23-02 planned (`bcf10e3`)

`docs/plans/m23-webhooks-bulk-export.md`, written against `6d1e1bf` with a
`file:line` on every claim. The check changed the order the milestone's summary
suggests: the webhook half needs a delivery pipeline that does not exist (no
sender in the worker, no HTTP client in the app crate, a signing helper that is a
SHA-256 prefix rather than HMAC), while the export half reuses the query engine,
the queue, the blob store and the grant path. So: queue fairness → export →
delivery → watches, with §20.4's weighted aging explicitly deferred.

## The bulk-export stub, reviewed

The stub landed uncommitted: `JobKind::BulkExport`, a worker arm, and `run_bulk`
returning `Fatal("…not yet implemented")`.

Holds: `parse`/`as_str` are symmetric; a malformed payload is `Fatal`; the stub is
unreachable (nothing enqueues the kind, `POST /jobs` takes only `maintenance`); no
migration is needed (`jobs.kind` is unconstrained `TEXT` in both dialects); no
frontend surface maps kinds; no test enumerates them.

Two defects, both from the change:

1. `sweep`'s doc comment was left above `run_bulk` (`exports.rs:799-814`), so
   `run_bulk`'s rustdoc opens with "Delete exports past their retention window"
   and `sweep` has none — and the new function sits under the "Retention" banner.
2. `known_kinds()` (`routes/jobs.rs:371-383`) lists six kinds by hand and its test
   asserts `len() == 6`, so the seventh variant left both passing. No callers
   outside the file, so no live bug — a guard that cannot fail.

Gates with the stub in the tree: **1228 passed / 0 failed / 13 ignored** across 47
binaries, `test_exit=0`. The only warning in the run is not from the stub:
`crates/app/tests/milestone_22.rs:1931` has an unused `items` binding, from the
other workstream in the tree.

## The bulk-export stub, second pass

The first review's doc-comment finding is fixed (`run_bulk` moved above the
retention banner, `sweep` documented again). The kind-list finding is half fixed:
`known_kinds()` now derives from one `ALL_KINDS` list and the test asserts the
wire-name round-trip for every entry — which also restored three kinds the old
hand-written list had dropped (`UpdateCheck`, `Derivative`, `Narration`) — but the
list is a `const` array whose doc comment claims new variants are "a compile error
everywhere it must be handled". A four-variant mirror compiles, warns and passes
with a variant missing from the list, so the claim is false; the fix is a
match-forced index plus a filled-index assertion.

`ResourceClass { Interactive, Bulk }` and `JobKind::resource_class()` also landed,
exhaustively matched, exactly as Phase 1 designed — with no consumer yet, so the
queue is still priority-only.

Gate with all of it in the tree: 1228 passed / 0 failed / 13 ignored, 47 binaries,
`test_exit=0`; the only warning is `crates/app/tests/milestone_22.rs:1931`'s unused
`items`, from the other workstream.
