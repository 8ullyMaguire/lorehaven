<script lang="ts">
  /**
   * New topic creation form (spec §35.3).
   *
   * Lets a writer start a discussion with a chosen thread mode (Plain by
   * default). Mode selection is only available when creating the to...[truncated]